
const rawRestaurantData = require('./restarauntData.json');
const userData = require('./userData.json')

const seedData = rawRestaurantData.map((raw) => { dba, boro, cuisine_description, latitude, longitude })


const seedDatabase = async () => {
bulkcCreate*(seedData)





process.exit(0);
}

seedDatabase()